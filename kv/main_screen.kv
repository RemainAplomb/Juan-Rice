#===========================================================================================================#

#    Developer:
#        Dibansa, Rahmani
#    Designer:
#        Garcia, Bhee Jay 
#    
#    Brief description of the program:
#        This program is a monitoring system for a smart vending machine, developed for an undergraduate thesis.

#===========================================================================================================#

#========== MAIN SCREEN SCREEN ==========#
#	The screen is defined within the MainScreen class.
#	The root layout of the screen is a FloatLayout.
#	The canvas.before instruction sets a background image for the screen.
#
#	Five ImageButton widgets are defined for various actions:
#		- "Refill" button: It has a unique ID, image source, size hint, 
#		  position hint, stretching behavior, and event handling for button 
#		  release to navigate to the refill screen.
#		- "Sales" button: It has a unique ID, image source, size hint, position 
#		  hint, stretching behavior, and event handling for button release to 
#		  navigate to the sales screen.
#		- "Rice Status" button: It has a unique ID, image source, size hint, 
#		  position hint, stretching behavior, and event handling for button 
#		  release to navigate to the rice status screen.
#		- "Notifications" button: It has a unique ID, image source, size hint, 
#		  position hint, stretching behavior, and event handling for button 
#		  release to navigate to the notification screen.
#		- "Go Back" button: It has a unique ID, image source, size hint, position 
#		  hint, stretching behavior, and event handling for button release to 
#		  navigate back to the machine screen.

This screen provides buttons for different actions, such as refill, sales, rice status, notifications, and going back to the machine screen. The buttons are styled and positioned accordingly.
<MainScreen>:
	FloatLayout:
		canvas.before:
			Rectangle:
				pos: self.pos
				size: self.size
				source: "resources/backgrounds/login_background.png"
		
		ImageButton:
			id: M_refill_BTN
			source: "resources/buttons/refill.png"
			#background_color: 0, 0, 0, 0
			size_hint : 0.5, 0.08
			pos_hint: { "x" : 0.265 , "y" : 0.64 }
			allow_stretch: True
			keep_ratio: False
			on_release:
				app.on_enter_refillScreen()

		ImageButton:
			id: M_sales_BTN
			source: "resources/buttons/sales.png"
			#background_color: 0, 0, 0, 0
			size_hint : 0.5, 0.08
			pos_hint: { "x" : 0.265 , "y" : 0.5 }
			allow_stretch: True
			keep_ratio: False
			on_release:
				app.on_enter_salesScreen()
		
		ImageButton:
			id: M_riceStatus_BTN
			source: "resources/buttons/status.png"
			#background_color: 0, 0, 0, 0
			size_hint : 0.5, 0.08
			pos_hint: { "x" : 0.265 , "y" : 0.36 }
			allow_stretch: True
			keep_ratio: False
			on_release:
				app.on_enter_riceStatusScreen("rice")
		
		ImageButton:
			id: M_notifications_BTN
			source: "resources/buttons/notifications.png"
			#background_color: 0, 0, 0, 0
			size_hint : 0.5, 0.08
			pos_hint: { "x" : 0.265 , "y" : 0.22 }
			allow_stretch: True
			keep_ratio: False
			on_release:
				app.on_enter_notificationScreen()
		
		ImageButton:
			id: M_goBack_BTN
			source: "resources/buttons/goBack.png"
			#background_color: 0, 0, 0, 0
			size_hint : 0.5, 0.08
			pos_hint: { "x" : 0.265 , "y" : 0.08 }
			allow_stretch: True
			keep_ratio: False
			on_release:
				app.root.current = "machine_screen"
				#app.on_MainScreen_signout_BTN()